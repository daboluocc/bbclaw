# ESP-IDF
build/
sdkconfig
sdkconfig.old
sdkconfig.bak
sdkconfig.local*
!sdkconfig.defaults
!sdkconfig.defaults.esp32s3

# ESP-IDF Component Manager（idf.py 根据 idf_component.yml 拉取）
managed_components/

# 本地生成（见 Makefile：gen-lv-font 等）
# generated/  # 已提交到 git，CI 需要

# make monitor-log、sim-export-feedback 等
.cache/

# macOS/SDL2 LVGL 预览
simulator/build/

# CI writes the release tag here for esp_app_desc.version; never commit (would pin/stale the version)
version.txt

# 隔离构建目录（并行会话时不碰共享 build/，用 idf.py -B build-<name>）
build-*/
