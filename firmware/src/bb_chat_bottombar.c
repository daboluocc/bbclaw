/**
 * CHAT bottom bar (Phase 3 skeleton).
 * Real implementation is currently in bb_lvgl_display.c's create_ui().
 */
#include "bb_chat_bottombar.h"

void bb_chat_bottombar_create(lv_obj_t* parent, int y, int width) {
  (void)parent;
  (void)y;
  (void)width;
  /* Phase 3: no-op */
}

void bb_chat_bottombar_set_session_id(const char* session_id) {
  (void)session_id;
}

void bb_chat_bottombar_set_cwd_name(const char* cwd_name) {
  (void)cwd_name;
}
