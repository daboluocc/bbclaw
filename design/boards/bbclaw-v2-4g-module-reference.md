# BBClaw 4G 版 — 模组选型与集成参考（2026-08-02）

> 来源：小蓝鲸物联科技《Mini核心板》文档（飞书 wiki，2025-07-11 修订，V0.001 / Jerry / Aaron）
> + 中移物联 ML307A 官方子型号表 + 合宙 Air780EG 硬件手册。
> 相关：`bbclaw-v2-pcb-layout-review.md`（当前 2 层版已定稿）。

## 1. 结论先行（2026-08-02 拍板）

| 决策 | 结论 |
|---|---|
| **模组 vs 板载裸芯片** | **买核心板模组**——理由见 §4，风险差距是数量级的 |
| **选哪颗芯片** | **ML307A-DCLN**（标准版，无定位、无语音） |
| **选哪块核心板** | **✅ Mini 核心板**（24.2×20.0×6.0mm，2排×6PIN 2.54mm）——**用户拍板：现有选型直接用**，
不再等 Tony/Tiny 补规格书。§8 的 Tony/Tiny 比较仅作记录，不采用（缺完整官方文档，Mini 已有全套规格书可直接设计） |
| **供电接法** | **VBAT 经 P-MOS 负载开关 → 核心板 BAT 脚**（不走 +5V，见 §5） |
| **UART** | 直连 ESP32-S3，**无需电平转换**（模块 UART 就是 3.3V TTL） |
| **ESP32 接口脚** | 4G_RXD=IO41、4G_TXD=IO42、4G_PWR_EN=IO2（见 §6） |

## 2. ML307A 子型号（官方图，纠正早期误读）

| 型号 | 含义 |
|---|---|
| **ML307A-DCLN** | **标准版**（无定位、无语音）← **本项目选这个** |
| ML307A-DSLN | 标准版 + **语音** |
| ML307A-GCLN | 标准版 + **定位** |
| ML307A-GSLN | 标准版 + 语音 + 定位 |

- 第 1 位：**D = 无定位 / G = 有定位**
- 第 2 位：**C = 无语音 / S = 有语音**（S 版内存也更大，才带 MIC/SPK/TTS/PCM/LCD/摄像头）

**BBClaw 选 DCLN 的理由**：音频走 ESP32 的 I2S，4G 只当数据管道；定位是死重（多一根天线、多耗电）。
⚠️ **若将来要让 4G 模块直接打电话（而非走云端），必须换 DS/GS。这个岔路现在定比返工便宜。**

## 3. Mini 核心板规格（关键数据）

| 项 | 值 |
|---|---|
| **外形尺寸** | **24.2 × 20.0 × 6.0 mm**（长×宽×高） |
| **对外接口** | **插针式，2 排 × 6PIN，脚间距 2.54mm，排间距 21.30mm** |
| 定位孔 | 四角各一（机械图可见） |
| **天线** | **50Ω 阻抗，IPEX 一代座（板上已做好）** |
| SIM 卡 | NanoSIM，**插拔卡座在核心板上** |
| **串口** | **UART TTL 3.3V**，默认 115200，可改 |
| 通信制式 | TDD-LTE / FDD-LTE，全网通（移动/电信/联通） |
| 工作频段 | TDD: B34/B38/B39/B40/B41；FDD: B1/B3/B5/B8 |
| 理论速率 | FDD 下行 10Mbps / 上行 5Mbps；TDD 下行 9Mbps / 上行 3Mbps |
| 工作温度 | −30 ~ +75°C |
| 兼容模组 | ML307C / ML307R / **ML307A** / ML307N / Air780E 系列 |

### 线序定义（**只有 6 个信号**）

| 脚 | 说明 |
|---|---|
| **BAT** | 电池供电脚，**3.4–4.5V（推荐 3.7V）** —— 供电方式一 |
| **VIN** | 电源供电脚，**5–12V（推荐 5V）** —— 供电方式二 |
| **EN** | 电源使能：用 BAT 供电时**可悬空**；用 VIN 供电时**必须与 VIN 短接** |
| **RXD** | 数据接收，TTL 3.3V |
| **TXD** | 数据发送，TTL 3.3V |
| **GND** | 地 |

> 同系列还有 **Tiny 核心板**（比 Mini 更小）和 Micro 核心板（更大）。
> 若 Tiny 也支持 ML307A，值得评估——能进一步省面积。**待查。**

## 4. 为什么不做板载裸模组

**核心板已经把风险最高的三块全部封装掉了**：

| 风险项 | 板载裸模组要自己做 | 用核心板 |
|---|---|---|
| **50Ω 射频阻抗** | 2 层 1.6mm FR4 上 50Ω 微带线需 **≈2.9mm 线宽**——40mm 宽的板子上不可行，**强制上 4 层** | ✅ 核心板内部做好，我们只接 6 根低速线 |
| **射频匹配网络** | π 型匹配要调，**没有网分仪就是盲调** | ✅ 已做好 |
| **SIM 卡座 + ESD** | 用户可插拔口，CLK/DATA/RST 要短 + ESD 阵列 | ✅ NanoSIM 座已在核心板 |
| 天线座 | 自己布 50Ω 到 IPEX | ✅ IPEX 已在核心板 |

**我们板上只剩：2 排 6PIN 排针 + 1 个负载开关 + 若干电容。零 RF 设计。**

**转板载的三个前提**（同时满足才划算）：① 4G 版软硬件都验证通过；② 已决定上 4 层板；③ 走量产、单板成本开始重要。

## 5. 供电接法（这是集成的关键决策）

文档给了两种参考电路：

### 方式一（VIN 5–12V）
`MCU_GPIO1 → R47 1k → SS8050 NPN（R48 47k 下拉）→ 控制 +5V 到模块 EN`

❌ **BBClaw 不用这条**：BBClaw 的 +5V 来自 SX1308 升压（3.7V→5V），
4G 峰值时要从 5V 轨抽 ≈1.4A，**SX1308 供不动**（其开关电流上限约 2A，5V 输出实际约 1.2A）。

### 方式二（BAT 3.4–4.5V）← **BBClaw 用这条**
`MCU_GPIO1 → R47 1k → SS8050 NPN（R49 10k 下拉）→ 驱动 AO3401 P-MOS 高侧开关 → 3.7V 电池 → 模块 BAT`

✅ **理由**：
1. **突发电流直接由电池提供**，不经过升压级——SX1308 完全不受影响
2. 无转换损耗
3. 正是文档推荐的电池供电做法
4. AO3401 是 P-MOS，与 BBClaw 已有的 AO3400（N-MOS，马达驱动）是互补件，好采购

⚠️ **两个必须核的点**：
- **电压下限**：模块 BAT 下限 **3.4V**，而 BBClaw 电池放到 3.0V。低于 3.4V 时 4G 会掉线/复位。
  → 固件要把 **4G 的低压关断线设在 ~3.5V**（BBClaw 现有 `BBCLAW_POWER_BATTERY_EMPTY_MV 3300` 要区分对待）
- **电芯脉冲能力**：4G 峰值 ~1.5A（Air780EG 官方数：峰值 1.5A、最大发射持续 ~700mA，Cat.1 同量级）。
  requirements 里「充电电流待确认电池容量 ≥1600mAh」这条本来就没闭环，**加 4G 后还要满足 ~1C 脉冲**。
  另需在模块 BAT 脚就近加 **大容量 bulk 电容**。

## 6. 与 ESP32-S3 的接口（BBClaw 现有资源够用）

| 信号 | ESP32-S3 脚 | 说明 |
|---|---|---|
| 4G_RXD ← ESP TX | **IO41** | 砍掉 SW4/SW5 后释放的空闲脚 |
| 4G_TXD → ESP RX | **IO42** | 同上 |
| 4G_PWR_EN | IO2 或 IO45 | 驱动 P-MOS 负载开关 |
| GND | — | 共地 |

**无需电平转换**（模块 UART 就是 3.3V TTL）。

### 软件：有现成驱动，且正是同类场景

**[`78/esp-ml307`](https://github.com/78/esp-ml307)** —— 作者虾哥（Terrence, tenclass），**源自 `xiaozhi-esp32`**：
- 支持 **ML307A / ML307R / EC801E / NT26K**（**Air780EG 不在列表**）
- MQTT / HTTP / TCP / UDP / **WebSocket**（BBClaw 云端正是 WS）
- v3.5：低功耗模式 + **DTR/RI 唤醒**、模块自动识别

**这是选 ML307A 而非 Air780EG 的决定性理由**——省掉整个 4G 驱动层开发。

## 7. 板级影响（待定）

- 核心板占板 **24.2 × 20.0mm = 4.84 cm²**；BBClaw 当前顶层最大**连续**空闲矩形仅 ~1.6 cm²
  → **板子必须放大，或模块移到背面**（但背面有电池，需机构确认）
- **高度 6.0mm + 排针座高**（2.54 排母约 2.5mm）→ 整机厚度增加 **~8.5mm**，手持设备要重新评估
- 4G 天线是**第二根天线**（板上已有 WROOM-1U 的 IPEX），外壳要留两个天线位

## 8. 补充：同店 Tony / Tiny 产品线（2026-08-02 追加）

用户提供了该店（小蓝鲸物联）另外两个商品截图，均支持 ML307A：

| 产品线 | 尺寸 | 线序（商品图标注） |
|---|---|---|
| **Tony 核心板**（B款线序） | 20.7 × 19.4mm，2.54mm 间距 | `VIN - GND - TXD - RXD - EN - BAT` |
| **Tiny 核心板**（A款线序） | 20.7 × 19.4mm，2.54mm 间距 | `EN - RXD - TXD - GND - VIN - BAT` |

### 关键发现：A 款与 B 款是同一圈引脚，只是描述方向不同

把两串按环形核对：B 从 `EN` 起点、**反方向**读一遍 → `EN → RXD → TXD → GND → VIN → BAT`，
与 A 款**逐字重合**。即 A/B 不是两套接法，而是**同一物理引脚圈，一个顺时针一个逆时针描述、起点不同**。

支持这一判断的另一条证据：**Tony 与 Tiny 尺寸完全一样（20.7×19.4mm）**——
两条本该是不同产品线，尺寸却分毫不差，大概率是**同一块物理板子挂了两个 SKU/名字**。

⚠️ **此判断来自商品图反推，不是官方原理图**，逻辑自洽但未 100% 验证。
落地前必须：选定具体 SKU 下单 → 收货后拍丝印照片 → 核对实物引脚位置再画载板封装。

### Tony/Tiny vs Mini 对比

| | 尺寸 | 面积 | 高度 |
|---|---|---|---|
| Mini（§3，来源飞书官方文档） | 24.2×20.0×6.0mm | 4.84 cm² | 6.0mm（已知） |
| **Tony/Tiny** | **20.7×19.4mm** | **4.02 cm²（省 ~17%）** | **未知（缺项）** |

Tony/Tiny 更小，若高度也不大，是比 Mini 更优的候选——**但目前只有 Mini 有完整官方规格书**
（阻抗/SIM卡/供电范围/温度范围等），Tony/Tiny 的这些参数还没有来源，只能先假设与 Mini 同平台。

## 9. 待办

1. ⬜ **下单前先确认 Tony/Tiny 的完整规格书**（供电电压范围、峰值电流、高度、SIM卡座、天线座类型）——
   目前只有商品图，没有等同于 Mini §3 的正式文档
2. ⬜ 收货后**拍丝印照片核对实际引脚位置**，确认 A/B 线序同圈的推断
3. ⬜ 查 ML307A **峰值电流**官方数（文档指向 ML307A 子文档，未展开）
4. ⬜ 定电芯规格（容量 + 脉冲能力）
5. ✅ ~~决定板子放大方案 or 背面安装 → 再开 `bbclaw-v2-4g` 工程~~——工程已建（2026-08-02），
   见 §10 复刻记录

## 10. `bbclaw-v2-4g` 工程复刻记录（2026-08-02）

### 排障：easyeda-agent 批量写操作长时间失败 → 根因是 uuid 语义误用

新建工程后，`sch place` 批量重放持续 `DISPATCH_FAILED / connector did not respond`，
排查了近 2 小时，依次排除：daemon 进程状态、windowId 过期、多标签并发、连接心跳、
doc-switch guard、页面是否"活"（用户交互画布）——**均不是根因**。

**真正原因**：`sch list --include-pins` 读回的已放置器件的 `component.uuid` /
`device.uuid`，**不是**可以拿去 `sch place --uuid` 跨工程复用的"设备库 uuid"。
两者字符串不同（例如 U8 Type-C：源工程读回 `79dccae1...`，但 `lib search` 查到的
真实库 uuid 是 `8c95ad7c...`）。用前者 place 会稳定超时（可能触发某种版本快照/
供应商绑定解析失败），用后者秒开。

**结论/以后适用**：**跨工程复刻器件，必须对每个唯一料号重新 `lib search` 取 uuid，
不能直接复用源工程 `sch list` 读回的 component/device uuid**——即便两个字段
数值相同、看起来像是"标准库 uuid"。

### 复刻方法（已验证可行，供后续页面复用）

1. 源工程 `sch list --page <name> --stay --include-pins --include-bbox` 逐页导出 JSON
2. 汇总所有唯一 `manufacturerId`，用 Python + `subprocess`（**不要用 bash 循环**，
   会莫名卡死；`easyeda lib search` 单条 15s 超时保护）逐个 `lib search` 建立
   `manufacturerId → 真实uuid` 映射表
3. 用映射表生成逐页 shell 脚本：`sch place`（真实 uuid + 源坐标/旋转/镜像/位号）
   后跟 **`sleep 0.5`**（连续无间隔调用会让页面主线程繁忙、错过心跳、被扩展判定
   "3 pings unanswered → reconnect" 强制断线），再 `sch autoconnect` 逐引脚接线
4. **不要用 `easyeda apply` playbook 引擎**——它内部的 `meta.doc` 自动切页动作会与
   紧接着的第一条写操作竞态，稳定超时；改用 per-step `--doc` flag 或干脆用原始
   shell 脚本（本项目最终采用后者）
5. 跑完一页后 **回读校验**而非信任脚本里的成功/失败字样（`| tail -1` 截到的可能是
   autoconnect 候选拒绝详情行，不是真正的结果行）：
   `sch list --include-pins` 统计"已接线引脚数 / 总引脚数"，核对未接线的是否
   在源数据里本来就是 `noConnected=true`
6. 对源数据里本就悬空的引脚补 `sch no-connect`，再跑
   `sch bridge-check` + `sch check` + `sch layout-lint` 三件套确认 0 问题

### 进度（2026-08-02 四页原理图全部复刻完成）

| 页 | 器件 | 状态 |
|---|---|---|
| 01-POWER | 34件/97脚 | ✅ bridge-check 0 / check 0 findings / layout-lint gate passed |
| 02-MCU | 10件/61脚 | ✅ bridge-check 0 / check 0 findings / layout-lint gate passed |
| 03-USB-DL | 9件/38脚 | ✅ bridge-check 0 / check 2 findings（丝印重叠，非电气）|
| 04-IO-HMI | 16件/63脚 | ✅ bridge-check 0 / check 1 finding（R23 侵入图签区，继承自源工程既有布局，非电气）|
| 05-4G-MODULE（新页） | — | ⬜ 待做，见 §5/§6 电路设计 |

**四页电气 100% 干净**：0 短路、0 悬空引脚、0 孤儿线。剩余 3 处 WARN 全部是
丝印/图签视觉重叠，不影响电路，留到收尾统一处理。

### 复刻过程中额外踩的坑（补充记录）

1. **同一 manufacturerId 在不同 lib search 结果里，symbol 引脚编号可能不同**——
   R23（0603电阻）复刻后 pin1/pin2 的物理位置与源数据对调了（电阻无极性，
   电气不受影响，但按"标号"直接连线会连错位置）。**教训：批量复刻后，凡是
   autoconnect 报错或结果可疑的，必须回读当前工程里的真实 pin 坐标，
   不能假设新工程的坐标/编号与源工程逐一对应。**

2. **`sch place --rotation 270` 实际落地可能变成 90°**（等效镜像 180°）——
   U4（拨轮）复刻后 rotation 从源数据 270° 变成 90°，所有引脚坐标随之偏移。
   直接用源数据坐标做 `autoconnect --x --y` 会打偏（表面显示✓成功，但
   `bridge-check` 也测不出来，因为它连的是空白点而非真实 pin——**必须用
   `sch list --include-pins` 现读真实坐标核对，不能信任 place 调用的
   rotation 参数被原样保留**。

3. **两脚间距很小的器件（如 0603 电阻两脚仅 40mil）批量 autoconnect 容易
   共线合并成 bridge（真短路）**——需要 `bridge-check` 逐页复核，
   发现后 `sch prim-delete` 整棵线树重连，用大幅不同的 `--offset-min/--offset-max`
   参数强制两条线走不同路径。

4. **`sch autoconnect --pin "X:N"` 遇到 pin 引用歧义时会静默失败**
   （pinNumber 和另一个 pin 的 pinName 字符串相同）——批量脚本里这类失败
   不会中断整体流程，**必须逐页做"接线率核对"（已接线引脚数/总引脚数），
   不能只看脚本自己打印的 ok/FAIL 计数**（那只反映 place 是否成功，不反映
   autoconnect 是否真的接上）。
