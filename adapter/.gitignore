# adapter build artifacts and runtime data — none of these belong in git
bin/
dist/
releases/
tmp/
*.tmp

# all dotenv variants — only .env.example may be tracked
.env
.env.*
!.env.example

# the binary itself, in case someone builds in adapter/ root
bbclaw-adapter
