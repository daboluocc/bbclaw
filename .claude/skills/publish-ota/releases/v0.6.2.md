# Release v0.6.2 (2026-07-11)

## 命令
git tag v0.6.2 && git push origin v0.6.2

## 前置状态
- git: 3fb627f (main, pushed)
- 上一个 tag: v0.6.1

## 关键改动(ADR-047 低功耗)
- feat(firmware): bbclaw 生产板息屏——关背光 GPIO14 + ST7789 DISPOFF(新 bb_display_control_st7789.c)
- feat(firmware): 息屏休眠灭状态灯(用户反馈:屏黑灯还亮)
- feat(firmware): CPU 自动 light-sleep + DFS(bb_pm)——⚠️ 仅 sdkconfig.pm,不进 OTA 生产配置
- 以及 v0.6.1 以来累积的手表 bring-up / 录音 / 触摸 UI / adapter_v2 串轮修等

## CI / OTA
- run: 29151894429
- firmware OTA: {"ok":true, version v0.6.2, isActive:true} — 已推 OTA server 并置为 active
- ⚠️ adapter / adapter-v2(windows) 构建失败(既有 CI 问题,与固件无关)→ publish(GitHub Release)不完整,
  但 firmware OTA 已在 build-firmware job 内独立推送成功。adapter 二进制本次未发布,需另修 adapter CI。

## 真机验证(发版前,连着的 bbclaw 板 usbserial-21120)
- OCT PSRAM 起动正常无 boot loop;息屏(1→2 + ST7789 panel OFF)+ 唤醒 + 灭状态灯 均实机确认
- OTA 成果物(sdkconfig.bbclaw.latest 无 PM)单独 build+flash 验证:bb_pm no-op、息屏有效

## 回滚计划
- make boot-recover 回 factory;再发更高版本(单 active 自动停用旧版)
