# Release v0.5.19 (2026-06-27)

## 发布方式
灰度发布（本地脚本 `release_local.sh`，直推 OTA，无 CI、无 GitHub Release）。

## 命令
```bash
# 前置：备份 bench sdkconfig（脚本会覆盖成 bbclaw.latest），发后还原
cp firmware/sdkconfig /tmp/sdkconfig.bench.bak
export IDF_PATH=/Volumes/1TB/esp/esp-idf IDF_TOOLS_PATH=/Volumes/1TB/dev-tools/.espressif
source "$IDF_PATH/export.sh"
export OTA_ADMIN_KEY=$(ssh root@daboluo.cc "grep '^CLOUD_OTA_ADMIN_KEY=' /opt/bbclaw-cloud/config/cloud.env | cut -d= -f2-")
cd firmware && ./scripts/release_local.sh v0.5.19
cp /tmp/sdkconfig.bench.bak firmware/sdkconfig   # 还原 bench 配置
```

## 前置状态
- git: `cbfed87` (main) — ⚠️ **未 push 到 origin**（bbclaw-reference 同样有 4 个未推 commit）
- IDF: v5.5.2-598-gbc7a18fe63
- board config: `sdkconfig.bbclaw.latest`（bbclaw 板 / OCTAL PSRAM / cloud_saas / 生产 URL）
- firmware size: **2.32 MB**（0x2517f0；分区 0x280000，7% free）
- 上一个 active: v0.5.18

## 关键改动（自 v0.5.18 起）
- `1072ff4` feat(firmware): 消费 adapter 权威 turn.committed/superseded 对账（ADR-040 Layer-2）
- `b594afd` feat(firmware): 顶栏「未发送」状态指示——撤销/打断未回复的回合（ADR-040）
- `3b43ac8` fix(firmware): barge-in 不再撤回 adapter 已提交的回合（ADR-040 Layer-1）
- `8b160d8` feat(firmware): 锁屏待机第一次按键只唤醒到挂锁页，不直接录音验证（另一会话）

> 设计依据：`design/decisions/ADR-040-device-adapter-turn-sync.md`

## OTA 服务端
- 推送到：`https://bbclaw.daboluo.cc/v1/ota/flash-bundle`
- bundle id：`bundle-v0.5.19-1782539504`，**isActive: true**
- app sha256：`1fb731029dddbf4d8c49cb1b2ba895df23041b14815dc99407249f6ab09a0d0d`（2430960 B）
- bootloader/partition/otadata 与历史版本同 sha（未变）

## 配套（Layer-2 端到端需一起到位）
- ✅ 云端已部署上线（`a4a9598`，daboluo.cc，转发 turn.committed/superseded）
- ⚠️ **adapter_v2 仍需 rebuild + 重启**（线上 PID 67002 是旧二进制，尚未发 turn.committed）
- 否则设备拉到 v0.5.19 后，Layer-1（撤销保留 + 未发送）生效，但 Layer-2 对账要等 adapter 重启

## 验证方式
- 设备 Settings → Check Update → 出现 v0.5.19
- 或 `curl https://bbclaw.daboluo.cc/v1/ota/flash-bundles?platform=esp32s3 | jq '.data[]|select(.isActive)|.version'` → `"v0.5.19"`
- 端到端：说一句 → 慢回复时按 PTT 说下一句 → 设备保留上一句 + 顶栏「未发送」（Layer-1）；adapter 重启后上一句被 turn.committed 对账确认（Layer-2）

## 回滚计划
- 严重问题：设备侧 `make boot-recover` 擦 otadata 回 factory 槽
- 云端：发 v0.5.20（或重推 v0.5.18 bundle 置 active）——「同平台单 active」会自动停用 v0.5.19
- ⚠️ 注意：本机 `firmware/build/` 现为 v0.5.19 bbclaw 产物；再烧 bench 前务必先 `make build`（sdkconfig 已还原成 bench，会重新编译），**别直接 `make flash`**

## 备注
- 本次未先在真机 flash 验证（用户选择直接灰度），靠灰度设备拉取后验证。固件仅 `-fsyntax-only` 编译验证过 + 本次 release build 成功。
